<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Essential Network & Background Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- Android TV Leanback & Touchscreen Hardware Configurations -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:banner="@drawable/app_banner"
        android:label="ServONVIF Monitor"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.ServONVIF"
        android:usesCleartextTraffic="true">

        <!-- Background Persistent Monitoring Service -->
        <service
            android:name=".service.MonitoringForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- TV & Tablet Boot Completed Receiver -->
        <receiver
            android:name=".service.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Universal Main Launcher Activity (Smart TV, Tablet & Mobile) -->
        <activity
            android:name=".ui.tv.TvMainActivity"
            android:exported="true"
            android:label="ServONVIF Central"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.ServONVIF">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Picture-in-Picture (PiP) Floating Alert Overlay -->
        <activity
            android:name=".ui.pip.PiPAlertActivity"
            android:exported="true"
            android:supportsPictureInPicture="true"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.ServONVIF" />

        <!-- Fullscreen Player Activity -->
        <activity
            android:name=".ui.tv.TvPlayerActivity"
            android:exported="false"
            android:label="ServONVIF Player"
            android:theme="@style/Theme.ServONVIF" />

        <!-- Settings Screen Activity -->
        <activity
            android:name=".ui.tv.TvSettingsActivity"
            android:exported="false"
            android:label="ServONVIF Configurações"
            android:theme="@style/Theme.ServONVIF" />

    </application>
</manifest>
