<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Theme.ServONVIF" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/blue_primary</item>
        <item name="android:windowBackground">@color/dark_bg</item>
    </style>

    <!-- Theme for TV with Leanback -->
    <style name="Theme.ServONVIF.TV" parent="Theme.Leanback">
        <item name="android:windowBackground">@color/dark_bg</item>
    </style>

    <!-- Theme for Floating Compact PiP Window (Overlay Corner Card) -->
    <style name="Theme.ServONVIF.FloatingPiP" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowElevation">16dp</item>
    </style>
</resources>
